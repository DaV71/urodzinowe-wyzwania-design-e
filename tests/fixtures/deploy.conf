# Fixture dla tests/deploy-lib.test.sh i `deploy.sh --dry-run --config tests/fixtures/deploy.conf`.
# Fikcyjne wartości (adres z puli dokumentacyjnej RFC 5737) — nic tu nie wskazuje na prawdziwy serwer.
SERVER_HOST=203.0.113.10
SSH_USER=deploy
SSH_PORT=2222
SSH_KEY=                    # puste = domyślny ~/.ssh

APP_DOMAIN=app.example.pl   # komentarz w linii
REPO_URL=https://github.com/przyklad/urodzinowe.git
REPO_BRANCH=main
DEPLOY_DIR=urodzinowe
GIT_TOKEN=token-testowy
HEALTH_TIMEOUT=180
